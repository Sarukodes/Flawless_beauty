
<div class="category-single {{ $i == 0 ? 'active' : '' }}">
    <div class="icon">
        <img src="{{ asset($cat->image) }}" alt="">
    </div>
    <div class="text">{{ $cat->title }}</div>
</div>











